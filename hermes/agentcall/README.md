# AgentCall bridge (Hermes)

Vendored from [pattern-ai-labs/agentcall](https://github.com/pattern-ai-labs/agentcall).

Hermes uses `scripts/python/bridge.py` plus `bin/*.sh` helpers. Set `AGENTCALL_API_KEY` in the repo root `.env` and rebuild/restart the `hermes` service.
